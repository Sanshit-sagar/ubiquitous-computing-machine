

import * as AccessibleIconPrimitive from '@radix-ui/react-accessible-icon';

const StyledAccessibleIcon = () => {

    return (
        <AccessibleIconPrimitive.Root />
    );
}

export const AccessibleIcon = StyledAccessibleIcon;
